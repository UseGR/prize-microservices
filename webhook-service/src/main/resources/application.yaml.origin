server:
  port: 8081
  ssl:
    enabled: false

application:
  kafka:
    topic: output-message-topic, output-callback-topic

spring:
  kafka:
    producer:
      bootstrap-servers: "127.0.0.1:9092"
      client-id: "webhook-producer"

bot:
  name:
  token: